<Page x:Class="ColourMemory.Views.PlayView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ColourMemory.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="PlayView">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="0.25*" />
			<ColumnDefinition Width="0.50*" />
			<ColumnDefinition Width="0.25*" />
		</Grid.ColumnDefinitions>
		<Grid Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="0.25*" />
				<RowDefinition Height="0.50*" />
				<RowDefinition Height="0.25*" />
			</Grid.RowDefinitions>
			<Viewbox Grid.Row="1" >
				<DataGrid Name="grid" Style="{StaticResource DataGridBase}" SelectedItem="{Binding ItemSelect, Mode=TwoWay}" ItemsSource="{Binding DataView}" />
			</Viewbox>
		</Grid>

	</Grid>
</Page>
